#include "main.h"
/**
 * print_alphabet- prints the alphabet in lowercase
 * Return: returns 0
 */
void print_alphabet(void)
{
	int alph = 97;
	
	while (alph <= 122)
	{
		_putchar('alph');
		alph++;
	}
	_putchar('\n');
}
