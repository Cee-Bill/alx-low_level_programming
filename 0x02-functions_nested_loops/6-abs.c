#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @r: expected integer value
 * Return: 0 for sucess
 */
int _abs(int r)
{
	if (r < 0)
	{
		r *= -1;
	}
	else
	{
		return (r);
	}
	return (r);
}
