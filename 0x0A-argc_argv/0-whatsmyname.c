#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 * @argc: integer param for argument count
 * @argv: char array holding app passed arguments
 * Return: 0 for success
 */

int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
