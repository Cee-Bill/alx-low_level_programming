#include <stdio.h>
/**
 * main - main program entry. Print a set of characters
 * Return: returns 0 for success
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
